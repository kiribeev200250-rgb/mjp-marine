Add phone call button to the presite (/go page) and update all pixels.

==================================================
PART 1 — ADD PHONE BUTTON TO PRESITE
==================================================

In app/go/page.tsx:

Add a phone call button that appears ABOVE the other link buttons
(it should be the most prominent — first button users see).

Fetch phone number from SiteConfig (already available via /api/config).

Button design:
- Same style as other presite buttons (full-width, gold border, sharp corners)
- Icon: phone SVG icon (inline)
- Label: "Call us now · Llámanos · Позвонить" (show based on detected language)
- href: tel:+34XXXXXXXXX (use phone number from SiteConfig)
- On click: track pixel events (see Part 2)

Also add phone button to /admin/presite Links tab:
- It should appear as the first item in the list
- Label editable, URL editable (tel: format)
- Platform type: "phone"
- Active toggle like other buttons

Add phone SVG icon in the platform icons map:
"phone": a simple phone handset SVG icon

==================================================
PART 2 — UPDATE ALL PIXELS FOR PHONE CLICK
==================================================

In lib/tiktok.ts and lib/facebook.ts add phone tracking.

When phone button is clicked on presite, fire ALL of these:

TikTok:
trackTikTokEvent('ClickButton', { content_name: 'Presite Phone Call' })
trackTikTokEvent('Contact', { content_name: 'Phone Call' })

Facebook:
trackFBEvent('track', 'Contact', { content_name: 'Presite Phone Call' })
trackFBEvent('track', 'Lead', { content_name: 'Phone Call' })

Also add phone tracking on main site contact section
wherever the phone number is displayed as a clickable link:

TikTok:
trackTikTokEvent('Contact', { content_name: 'Phone Number Click' })

Facebook:
trackFBEvent('track', 'Contact', { content_name: 'Phone Number Click' })

==================================================
PART 3 — ADD PHONE TO PRESITE ADMIN
==================================================

In /admin/presite Links tab:
- Add "phone" as a platform option in the dropdown
- Phone icon displayed in the list
- When platform = "phone", show hint text:
  "Format: tel:+34600000000"

==================================================
PART 4 — SEED DEFAULT PHONE LINK
==================================================

In prisma/seed.ts add a default phone PresiteLink:
{
  platform: "phone",
  label: "Call us",
  url: "tel:+34600165680",
  active: true,
  sortOrder: 0  // first in list
}

Run: npx prisma db seed -- if needed to add the new entry.

Run npm run build — 0 errors.{
  "permissions": {
    "allow": [
      "Bash(npx ts-node *)",
      "Bash(mkdir -p /Users/anastasiakurac/Desktop/sayt/app/api/admin/contacts/\\\\[id\\\\])",
      "Bash(mkdir -p /Users/anastasiakurac/Desktop/sayt/app/api/admin/subscribers/\\\\[id\\\\])",
      "Bash(curl -s -o /dev/null -w \"%{http_code}\" http://localhost:3000/admin/login)",
      "Bash(curl -s -o /dev/null -w \"%{http_code}\" http://localhost:3002/admin/login)",
      "Bash(curl -s -o /dev/null -w \" %{http_code}\" http://localhost:3002/)",
      "Bash(npm install *)",
      "Bash(npm run *)",
      "Bash(npx prisma *)",
      "Bash(npx tsc *)"
    ]
  }
}
